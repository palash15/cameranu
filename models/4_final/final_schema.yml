version: 2

models:
  
  - name: _9001_device_data
    description: "Final table for device data (mobile & desktop)"
    
    columns:
      - name: user_pseudo_id
        description: "One of the primary keys for this table"

      - name: session_id
        description: "One of the primary keys for this table"