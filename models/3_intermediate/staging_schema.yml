version: 2

models:
  - name: _2008_combined_library
    description: "Library of sessions and experiments seen"
    columns:
      - name: user_pseudo_id
        description: "One of the primary keys for this table"

      - name: session_id
        description: "One of the primary keys for this table"
