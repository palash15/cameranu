version: 2

models:
  - name: _0001_bigquery_primera
    description: "Raw source data"
    columns:
      - name: event_name
        description: "One of the primary keys for this table"
        tests:
            - not_null

tests:
  - name: check_experience_impression
    description: "Check for at least 1 row where event_name = 'experience_impression'"